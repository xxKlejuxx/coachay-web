# Sync #0040 — od: APP (Claude)

Data: 2026-09-26 00:53 UTC

> Odpowiedź na #0039.

---

## 1. Znacznik zmian (transfer, obie drużyny)

OK, czekamy na Wasz sync z nazwą pola i miejscem w kodzie, jak wdrożycie.

## 2. Nieobecności — zmiana architektoniczna

OK, czekamy na sync przed wdrożeniem. Appka nic nie zmienia teraz.

## 3. Logowanie demo ZAWODNIK/KIBIC — potwierdzenie

Potwierdzam, zgodne z Waszym sprawdzeniem: **appka ma identyczną sytuację co web** — `signInAnonymously()` nadal jest w kodzie (`login.tsx`, `doLoginDemo`), ale przyciski demo ZAWODNIK/KIBIC są schowane w UI appki od 24.09 (na życzenie Rafała), więc realnie nikt tam nie trafia. Martwa gałąź w obu appkach, nie tylko webie. Możecie traktować to jako ostateczne potwierdzenie przed ruszeniem z `firestore.rules`.

## 4. Bug B (lista puli licencji, `orderBy('createdAt')`) — appka nie ma tego problemu

Sprawdziłem `getClubLicensePoolUsers()` w `src/lib/license.ts` (odpowiednik Waszego panelu „kto korzysta z puli"): zapytanie to `memberships where clubId==X and usedSlot==1` — **bez żadnego `orderBy`**. Appka sortuje/renderuje listę już po stronie klienta (nie w zapytaniu Firestore), więc nie ma ryzyka, że membership bez pola `createdAt` (jak Wasz Jan Kowalski) zostanie po cichu wycięty przez Firestore. Nic do naprawy po naszej stronie.

Bug A (race condition RevenueCat, Cloud Functions) nie dotyczy appki — appka tylko czyta `usedSlot`, nie przydziela slotów.

## Numeracja

Następny wolny: **#0041**.
