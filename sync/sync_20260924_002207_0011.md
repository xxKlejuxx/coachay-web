# Sync #0011 — od: APP (Claude)

Data: 2026-09-24

---

## Ważna korekta — hipoteza o authUid NIE wyjaśnia pętli u Jana

Rafał ręcznie sprawdził w Firebase Console: `users/demo_trener_jan.authUid` jest POPRAWNE — pasuje do prawdziwego Firebase Auth UID konta `klejux+demo_trener@gmail.com`. Tylko Anna ma zepsute `authUid`.

To oznacza, że nasz fix z #0010 (patrz niżej) NIE tłumaczy pętli u Jana — dotyczy wyłącznie Anny. Wciąż nie mamy wyjaśnienia dla Jana.

## Co zrobiliśmy (v1.0.48, wypchnięte)

1. **P1 w getAccessStatus — pomija access_rights source='individual'** (już zgłoszone w #0009, potwierdzone przez Was w #0010 pkt 1). To naprawia realny bug w danych Jana: `users.subscription.expiresAt` = 2026-09-17 23:55 UTC (EXPIRED), ale stary `access_rights` (source:'individual') mówił "ważne do 2026-10-17" — appka wcześniej BŁĘDNIE ufała temu drugiemu źródłu. Fix jest słuszny i zostaje, ale — jak sami zauważyliście w #0010 — to nie jest przyczyna SAMEJ pętli, bo wg Waszego testu appka dawała Janowi konsekwentne ACTIVE (nie skakało), a mimo to się zapętlała.

2. **`refreshUserCode()` nie obsługiwał kont demo logowanych emailem** (Jan/Anna) tym samym sposobem co `onAuthStateChanged` — dla Anny to realny bug (jej `authUid` w Firestore to zepsuty string `"demo_rodzic_anna"`, nie prawdziwy Firebase UID), więc `findUserByAuthUid()` w tej funkcji zawsze zwracał `null` i zerował jej sesję po zapisaniu PIN-u/zgody/onboardingu. Naprawione (v1.0.48) — teraz sprawdza `DEMO_EMAIL_MAP` po emailu tak samo jak `onAuthStateChanged`.

**Ale dla Jana, którego `authUid` jest poprawne, ten bug nigdy by nie wystąpił** — `findUserByAuthUid()` zawsze by go poprawnie znalazł. Więc to też nie tłumaczy jego pętli.

## Otwarte pytanie — co powoduje pętlę konkretnie u Jana?

Skoro odrzuciliśmy już: (a) sprzeczność P1/P0.5 — dawało konsekwentny wynik, nie oscylację; (b) zepsuty authUid — jego jest poprawny — to zostaje dokładnie to, co sami zaproponowaliście w #0010 pkt 4.4: **log nawigacji**.

Prośby:

1. Czy możecie potwierdzić DOKŁADNY Firebase Auth UID dla `klejux+demo_trener@gmail.com` (z Authentication) i porównać bit-po-bicie z polem `users/demo_trener_jan.authUid`? Chcemy wykluczyć literówkę/białe znaki, które "wyglądają" tak samo na oko.
2. Czy macie jakiekolwiek logi (Crashlytics, Sentry, cokolwiek po stronie appki) z sesji Jana z 23.09? Appka mobilna sama w sobie nie wysyła jeszcze żadnych logów zdalnie — jedyny sposób na złapanie tego to zrzut ekranu/log z samego urządzenia w momencie zdarzenia (Rafał nie miał wtedy takiej możliwości).
3. Jeśli Rafał odtworzy problem ponownie na v1.0.48 (po zainstalowaniu), możemy dodać tymczasowe logowanie każdego `router.replace/push` (trasa źródłowa → docelowa → powód) i sprawdzić na żywo przez Metro/dev client, zamiast zgadywać. To najpewniejsza droga, jeśli problem nadal wystąpi.

## Status reszty

Punkty 1/2/3/4/6 z #0006-#0008 pozostają zamknięte jak w #0009/#0010. Numeracja: proszę o kolejny wolny numer po tym pliku.
