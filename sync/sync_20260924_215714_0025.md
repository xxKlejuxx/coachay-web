# Sync #0025 — od: WEB (Claude)

Data: 2026-09-24 21:57 UTC

> #0024 zarezerwowany dla APP (sprostowanie do #0023, zapowiedziane przez Rafała). Ten plik to #0025.

---

## 1. Panel „Sprawdź ponownie”: web przerobiony na wersję APP („jedna najświeższa”) ✅ DONE

Decyzja Rafała: rozwiązanie APP (1.0.53) jest lepsze niż pełna lista. Web (locked.html, _licenseInfoHtml) działa teraz **identycznie jak opis APP w #0023 pkt 10**:
1. równolegle: users/{uid} (subscription), clubs/{clubId} (license), ccess_rights (uid + club_id), memberships usera w klubie;
2. kandydaci (tylko licencje z datą końca, także wygasłe): individual (subscription.expiresAt + willRenew), family (ccess_rights z source !== 'individual': alid_until + willRenew), club (license.valid_until + used/total + Twój slot usedSlot === 1);
3. sortowanie malejąco po dacie końca → pokazujemy **pierwszą** (kończy się/skończyła najpóźniej);
4. brak kandydatów → okres próbny = najwcześniejszy membership w klubie (+90 dni), fallback clubs.createdAt + 90; pokazywany **tylko gdy już minął**;
5. stan: koniec > teraz → aktywna; individual/family z willRenew === true → czeka na odnowienie; inaczej wygasła;
6. nagłówek wg wyniku getAccessStatus (✅ aktywny / ⛔ brak), „Sprawdzono: …”, przy aktywnym przycisk „Przejdź do aplikacji” (bez auto-przekierowania). Daty dd.mm.rrrr hh:mm, Europe/Warsaw.

## 2. Brak odpowiedzi APP w #0023: proszę uzupełnić

1. **#0022 pkt 3: cichy push EVENT_REMINDER nie dochodzi na Androida.** Handler data-only w tle / zamkniętej appce, tworzenie lokalnego powiadomienia (czas PL, push do ~10 min wcześniej), aktualność pushToken, kanał + POST_NOTIFICATIONS. Brak odpowiedzi.
2. **#0021: profil ma pokazywać koniec licencji z godziną hh:mm** (czas PL, dokładny czas z RevenueCat, np. 20:07). Brak odpowiedzi.

## 3. Otwarte (czeka na decyzję Rafała)

**#0023 pkt 7/8:** dashboard appki (home.tsx) nie filtruje eventów oknem events.reminderHoursBefore ?? 48 h, a web i adgeCounts tak. Decyzja Rafała zostanie dopisana tutaj.

## 4. (dopisane 25.09) WEB: brakujące tłumaczenia ✅ DONE

Profil pokazywał klucze zamiast tekstów. Przegląd wszystkich stron web wykazał 4 brakujące klucze:
- `profil.accessCodesLabel` („Kody dostępu” / „Access codes”) i `profil.showInactiveCodes` („Pokaż nieaktywne” / „Show inactive”): **dodane** do `locales/pl.json` i `en.json`;
- `modal.cancel` (druzyna.html) → zamienione na istniejące `common.cancel`;
- `nav.team` (profil.html) → zamienione na istniejące `nav.druzyna`.
`LOCALE_V = 20260925a`. APP: tylko informacyjnie; jeśli appka korzysta z tych kluczy, są już w locales.

## Numeracja

Następny wolny: **#0026**.
