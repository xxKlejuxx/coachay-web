# Sync #0007 — od: WEB (Claude)

Data: 2026-09-23 21:44 UTC

---

## Temat: Pętla po zalogowaniu na kontach demo (Jan, Anna) — przyczyna i zmiany

**Objaw:** w aplikacji mobilnej, po zalogowaniu na `demo_trener_jan` i `demo_rodzic_anna`, aplikacja zapętla się i nie dochodzi do ekranu start. Nie da się też wylogować. Firebase Auth przyjmuje logowanie, więc pętla dzieje się już po zalogowaniu, w logice dostępu.

### 1. Stan danych (23.09)

| | Jan | Anna |
|---|---|---|
| `users.subscription` | **EXPIRED**, `expiresAt` 2026-09-17 23:55 UTC | **EXPIRED**, 2026-09-17 23:55 UTC |
| `access_rights` (`source: 'individual'`) | `ar_demo_trener_jan_club_orly_praga`, **valid_until 2026-10-17** (zapisane jako STRING) | `ar_demo_rodzic_anna_club_orly_praga`, **2026-10-17** (STRING) |
| trial (`membership.trialEndsAt`) | minął (12.07.2026 / 2024) — ALE membership `mbr_trener_20260824_2494772` ze statusem **DELETE** ma `trialEndsAt` **2026-11-22** | minął |
| `usedSlot` | 0 na wszystkich | 0 na wszystkich |
| `clubs/club_orly_praga.license` | used **2**/2, do 2026-10-13 | j.w. |

### 2. Co się stało 17.09 (odtworzone z danych)

- 01:07 UTC (Jan) / 15:13 UTC (Anna): zakup miesięcznej licencji indywidualnej w aplikacji (najpewniej sandbox RevenueCat).
  - **APP zapisała `access_rights`** (`source: 'individual'`, +30 dni, `valid_until` jako tekst).
  - Webhook: `INITIAL_PURCHASE` → `users.subscription = ACTIVE`.
- ~40 min później: webhook `EXPIRATION` (sandbox: miesiąc ≈ 5 min, max 6 odnowień) → `users.subscription = EXPIRED`, `expiresAt` zaokrąglone do 23:55 tego dnia. Próba przydziału slotu klubowego → pula „pełna” → `usedSlot = 0`.
- **`access_rights` nikt nie zaktualizował** i nadal mówi „ważne do 17.10”.

W bazie są więc dwa sprzeczne zapisy tej samej licencji: `users.subscription` (EXPIRED, zgodne ze sklepem) i `access_rights` (ważne, nieaktualna kopia).

### 3. Dlaczego pętla (hipoteza do potwierdzenia przez APP)

Web do dziś wpuszczał Jana przez P1 (`access_rights`, source=individual). Aplikacja, jeśli w jednym miejscu liczy dostęp z pól `cached*` na membershipach (dla P1 nie ma żadnej kopii na membershipie, a `cachedUserSubscription` = EXPIRED), a w innym czyta `access_rights` jak web, dostaje dwie sprzeczne decyzje. Guard wpuszcza na start, start odsyła na blokadę, i tak w kółko.

Druga możliwa rozbieżność: trial z membershipu o statusie `DELETE` (Jan, `trialEndsAt` 22.11.2026), jeśli któraś ścieżka nie odrzuca DELETE/INACTIVE.

### 4. WEB — ✅ DONE (commit 474378f, Hosting wdrożony)

- `getAccessStatus()`, krok **P1: pomija `access_rights` z `source === 'individual'`**. Licencja indywidualna liczy się wyłącznie z P0.5 (`users.subscription`). W P1 zostają: licencja rodzinna (`slots_total`, w danych `source: 'personal'`), `admin_perpetual` i inne nie-indywidualne.
- `blocked.html`: testowy przycisk „Kup licencję indywidualną” ustawia teraz `users.subscription` (ACTIVE, 1 rok) zamiast `access_rights`.
- Oczekiwany efekt na web: Jan i Anna → ekran blokady (`club_pool_full`, bo licznik klubu 2/2) z przyciskiem wylogowania. Testerzy (`tester1` / `tester2`) → TRIAL do grudnia.

### 5. APP — prośby

1. **Sprawdzanie dostępu:** tak samo jak web, pomijać `access_rights` z `source: 'individual'`. Licencja indywidualna tylko z `users.subscription` / `cachedUserSubscription`. **Wszystkie miejsca w aplikacji, które decydują o dostępie (guard, ekran start, ekran blokady), muszą używać JEDNEJ i tej samej funkcji.**
2. **Zakup:** przestać zapisywać `access_rights` przy zakupie licencji indywidualnej. Webhook RevenueCat sam zapisuje `users.subscription` (także odnowienia i wygaśnięcie).
3. **Trial:** pomijać membershipy ze statusem `DELETE` / `REMOVED` / `INACTIVE` / `BLOCKED` przy szukaniu aktywnego triala.
4. **Ekran blokady = ekran końcowy:** bez automatycznego przekierowania na start, z przyciskiem „Wyloguj się”. Dziś użytkownik w pętli nie może się wylogować.
5. **Diagnoza:** proszę o wskazanie, które dwa miejsca w aplikacji dawały sprzeczne decyzje dla Jana. Odtworzenie: zalogować się jako `demo_trener_jan` (dane jak w tabeli wyżej).

Po wdrożeniu proszę o potwierdzenie w #0008.

### 6. Otwarte po stronie WEB (do decyzji Rafała, nie zrobione)

- `clubs/club_orly_praga.license.used = 2`, a slot ma faktycznie tylko 1 membership (`mbr_demo_trener_asystent`). Licznik rozjechany → wolnego slotu nie da się pobrać. Do korekty (2 → 1) plus propozycja: nocne zadanie przelicza `license.used` z faktycznej liczby `usedSlot == 1`.
- 4 indywidualne `access_rights` (Jan, Anna, tester1, tester2): usunąć, gdy APP wdroży punkty 1–2 (do tego czasu web je ignoruje).
- Ujednolicenie: `valid_until` jako timestamp (nie tekst), nazwa źródła licencji rodzinnej (`personal` → `family`).
- Trzy różne źródła końca triala (web: `createdAt + 90`, CF V1: `users.clubs_trial`, CF V2 / APP: `membership.trialEndsAt`) → ujednolicić do `membership.trialEndsAt`.
