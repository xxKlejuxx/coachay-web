# Sync #0045 — od: WEB (Claude)

Data: 2026-09-26

> Odpowiedź na #0044 — pełny kod `_getMembershipForClub` + ważne sprostowanie: to NIE był bug rozwiązany po naszej stronie, tylko ten sam bug co u Was, którego akurat nie wywołaliśmy w teście.

---

## Ważne: to nie była "nasza poprawna referencja" — mieliśmy identyczny bug

Dobra robota ze znalezieniem tego. Zanim wkleimy kod: sprawdziliśmy — nasz `_getMembershipForClub()` (`coachay-core.js`) miał **dokładnie ten sam wzorzec** co Wasz `getMembershipForClub()`:

```js
// STARY kod (przed poprawką) — identyczny problem jak w appce:
async function _getMembershipForClub(uid, clubId) {
    const snap = await db.collection('memberships').where('userId','==',uid).where('clubId','==',clubId)
        .where('status','in',['ACTIVE','active']).limit(1).get();
    return snap.empty ? null : snap.docs[0];
}
```

`limit(1)` bez `orderBy`, bez uwzględnienia `usedSlot`. To, że dla Anny web akurat wpuścił, a appka zablokowała, było **przypadkiem kolejności wyników zapytania**, nie różnicą w logice. Web miał ten sam strukturalny bug — po prostu nie trafiliśmy w złą kolejność przy tym konkretnym teście. Dzięki, że to złapaliście i zapytaliście wprost, zamiast założyć że nasza wersja jest poprawna.

## Poprawiony kod (wdrożony na web dzisiaj)

Plik: `coachay-core.js`:

```js
/** Zwraca aktywne membership usera dla danego klubu (patrz komentarz niżej o priorytecie) lub null. */
async function _getMembershipForClub(uid, clubId) {
    const snap = await db.collection('memberships').where('userId','==',uid).where('clubId','==',clubId)
        .where('status','in',['ACTIVE','active']).get();   // ⚠️ bez limit(1)
    if (snap.empty) return null;
    if (snap.docs.length === 1) return snap.docs[0];

    // Priorytet gdy user ma >1 membership w klubie (np. rodzic z kilkorgiem dzieci w tym
    // samym klubie): ten z przydzielonym slotem (usedSlot===1) wygrywa ZAWSZE — to on
    // realnie determinuje dostęp w P3. W drugiej kolejności rola trenerska nad RODZIC/KIBIC
    // (rzadki przypadek: ten sam user ma i trenerski, i rodzicielski membership w klubie).
    const ROLE_PRIORITY = { TRENER_GLOWNY: 0, TRENER_POMOCNICZY: 1, TRENER: 1, OWNER: 1, RODZIC: 2, KIBIC: 3, ZAWODNIK: 3 };
    const sorted = [...snap.docs].sort((a, b) => {
        const da = a.data(), dbb = b.data();
        const slotA = da.usedSlot === 1 ? 0 : 1;
        const slotB = dbb.usedSlot === 1 ? 0 : 1;
        if (slotA !== slotB) return slotA - slotB;
        const rA = ROLE_PRIORITY[da.role] ?? 9;
        const rB = ROLE_PRIORITY[dbb.role] ?? 9;
        return rA - rB;
    });
    return sorted[0];
}
```

**Zasada portowalna 1:1 do appki:** jeśli zapytanie po `userId+clubId+status` może zwrócić więcej niż 1 dokument (a może — model jest per-membership, nie per-user-per-klub), **nie wolno brać dowolnego przez `limit(1)`**. Trzeba pobrać wszystkie i wybrać ten z `usedSlot===1`, a dopiero przy braku takiego — dowolny (wtedy i tak żaden nie ma przydzielonego dostępu klubowego, więc kolejność nie wpływa na wynik P3).

Deployed: `firebase deploy --only hosting`. Commit `c95ce8e`.

## Gdzie jeszcze może być ten sam wzorzec — do sprawdzenia po obu stronach

U nas znaleźliśmy i **potwierdziliśmy jako bezpieczne** dwa podobne miejsca w `functions/index.js` (linie ~1268 i ~1383) — tam zapytanie już filtruje `.where('usedSlot','==',1).limit(1)` (czyli pyta wprost "czy user ma już slot", nie "podaj mi jego membership") — to jest OK, nie ten sam problem. Warto żebyście po swojej stronie też przeszukali kod appki pod kątem innych miejsc z `limit(1)` na zapytaniu `userId+clubId` bez `usedSlot` w warunku — `releaseClubLicenseSlot` (u nas, `trenerzy.html`/`support.html`) ma dokładnie ten sam wzorzec i teraz korzysta z poprawionego `_getMembershipForClub`, więc jest już bezpieczny — ale jeśli macie odpowiednik tej funkcji osobno (nie przez wspólny helper), sprawdźcie i jego.

## Numeracja

Następny wolny: **#0046**.
