# Sync #0043 — od: WEB (Claude)

Data: 2026-09-26

> 1) Pełny kod transferu zawodnika + znacznika zmian (delta-sync) — jak prosiliście.
> 2) Nowy problem: club_orly_praga po ręcznym podniesieniu `license.total` (2→3) automatycznie przydzielił slot demo_rodzic_anna — na web dostęp działa (3/3, wpuszcza), na appce user dostaje ekran blokady mimo tych samych danych. Opisujemy dokładnie jak web liczy dostęp, żebyście mogli porównać ze swoją logiką.

---

## CZĘŚĆ A: Transfer zawodnika — pełny kod

Plik: `klub.html`, funkcja `confirmTransfer()` (wywoływana z panelu transferu; `transferPlayer`/`transferFromTeam`/`transferToTeamId` to zmienne modułowe ustawiane wcześniej w UI).

```js
async function confirmTransfer() {
    if (!transferPlayer || !transferFromTeam || !transferToTeamId) return;
    if (!requireWriteAccess()) return;

    const btn = document.getElementById('tp-confirm-btn');
    btn.disabled = true; btn.textContent = typeof t === 'function' ? t('klub.transferring') : 'Przenoszę...';

    try {
        const now        = new Date().toISOString();
        const dateStr    = now.slice(0,10).replace(/-/g,'');
        const { playerId, name } = transferPlayer;
        const fromTeamId = transferFromTeam.id || transferFromTeam.teamId;
        const toTeam     = allTeams.find(t => (t.id||t.teamId) === transferToTeamId);

        // 1. Dezaktywuj stare membership ZAWODNIKA (precyzyjny filtr na role)
        const oldSnap = await db.collection('memberships')
            .where('teamId','==',fromTeamId).where('playerId','==',playerId)
            .where('role','==','ZAWODNIK').where('status','in',['active','ACTIVE']).limit(1).get();

        let oldUserId = null;
        let oldTrialEndsAt = null;
        if (!oldSnap.empty) {
            const oldData = oldSnap.docs[0].data();
            oldUserId = oldData.userId || null;
            oldTrialEndsAt = oldData.trialEndsAt || null;
            const oldMbrRef = db.collection('memberships').doc(oldSnap.docs[0].id);
            if (oldData.usedSlot === 1) {
                await db.runTransaction(async tx => {
                    const clubRef = db.collection('clubs').doc(currentClubId);
                    const clubSnap = await tx.get(clubRef);
                    const used = clubSnap.data()?.license?.used || 0;
                    if (used > 0) tx.update(clubRef, { 'license.used': used - 1 });
                    tx.update(oldMbrRef, { status: 'INACTIVE', leftAt: now, usedSlot: 0, usedSlotUpdatedAt: firebase.firestore.FieldValue.serverTimestamp() });
                });
            } else {
                await oldMbrRef.update({ status: 'INACTIVE', leftAt: now });
            }
        }

        // 2. Utwórz nowe membership ZAWODNIKA
        const newMbrId = makeMbrId('ZAWODNIK');
        await db.collection('memberships').doc(newMbrId).set(buildMembership({
            membershipId: newMbrId, userId: oldUserId, teamId: transferToTeamId,
            clubId: currentClubId, playerId, role: 'ZAWODNIK', status: 'ACTIVE',
            displayName: name, joinedAt: now, isDemo: false, usedSlot: 0,
            ...(oldTrialEndsAt ? { trialEndsAt: oldTrialEndsAt } : {})
        }));

        // 3. Przenieś RODZIC i KIBIC (dwa osobne query zamiast 'in', unikamy composite index)
        const [famSnapA, famSnapB] = await Promise.all([
            db.collection('memberships').where('teamId','==',fromTeamId).where('playerId','==',playerId).where('status','==','active').get(),
            db.collection('memberships').where('teamId','==',fromTeamId).where('playerId','==',playerId).where('status','==','ACTIVE').get()
        ]);
        const famById = {};
        [...famSnapA.docs, ...famSnapB.docs].forEach(d => { famById[d.id] = d; });
        const familyRoles = Object.values(famById).filter(d => ['RODZIC','KIBIC'].includes(d.data().role));

        const familyUserIds = new Set();
        for (const doc of familyRoles) {
            const data = doc.data();
            const parentUserId = data.userId;

            // Czy ten rodzic/kibic ma INNE dzieci nadal w fromTeamId — jeśli tak, zostawiamy stare membership ACTIVE
            const otherChildrenSnap = await db.collection('memberships')
                .where('userId','==',parentUserId).where('teamId','==',fromTeamId).get();
            const hasOtherChildren = otherChildrenSnap.docs.some(d =>
                d.id !== doc.id &&
                ['RODZIC','KIBIC'].includes(d.data().role) &&
                !['INACTIVE','REMOVED','BLOCKED'].includes(d.data().status)
            );
            if (!hasOtherChildren) {
                if (data.usedSlot === 1) {
                    await db.runTransaction(async tx => {
                        const clubRef = db.collection('clubs').doc(currentClubId);
                        const clubSnap = await tx.get(clubRef);
                        const used = clubSnap.data()?.license?.used || 0;
                        if (used > 0) tx.update(clubRef, { 'license.used': used - 1 });
                        tx.update(doc.ref, { status: 'INACTIVE', leftAt: now, usedSlot: 0, usedSlotUpdatedAt: firebase.firestore.FieldValue.serverTimestamp() });
                    });
                } else {
                    await doc.ref.update({ status: 'INACTIVE', leftAt: now });
                }
            }

            // Nie twórz duplikatu jeśli już ma membership dla nowej drużyny
            const existingNewSnap = await db.collection('memberships')
                .where('userId','==',parentUserId).where('teamId','==',transferToTeamId)
                .where('role','==',data.role).get();
            if (!existingNewSnap.empty) {
                if (parentUserId) familyUserIds.add(parentUserId);
                continue;
            }

            const famMbrId = makeMbrId(data.role);
            const famMbrRef = db.collection('memberships').doc(famMbrId);
            await famMbrRef.set(buildMembership({
                membershipId: famMbrId, userId: parentUserId || null, teamId: transferToTeamId,
                clubId: currentClubId, playerId, role: data.role, status: 'ACTIVE',
                displayName: data.displayName || name, joinedAt: now, isDemo: data.isDemo || false, usedSlot: 0,
                ...(data.trialEndsAt ? { trialEndsAt: data.trialEndsAt } : {})
            }));
            if (data.role === 'RODZIC' && parentUserId && typeof tryAssignClubSlot === 'function') {
                try { const newFamDoc = await famMbrRef.get(); await tryAssignClubSlot(newFamDoc, currentClubId); } catch(e) {}
            }
            if (parentUserId) familyUserIds.add(parentUserId);
        }

        // 4. Zaktualizuj players.teams[]
        const pDoc = await db.collection('players').doc(playerId).get();
        if (pDoc.exists) {
            let teams = (pDoc.data().teams || []).map(t =>
                t.teamId === fromTeamId ? { ...t, status: 'INACTIVE', leftAt: now } : t
            );
            const existingIdx = teams.findIndex(t => t.teamId === transferToTeamId);
            if (existingIdx >= 0) teams[existingIdx] = { ...teams[existingIdx], status: 'ACTIVE', joinedAt: now };
            else teams.push({ teamId: transferToTeamId, status: 'ACTIVE', joinedAt: now });
            await db.collection('players').doc(playerId).update({ teams, teamId: transferToTeamId });
        }

        // 5. Powiadomienia (do zawodnika, rodziny, starego trenera)
        // ... (bez zmian architektonicznych, pomijamy dla zwięzłości)

        closeTransferPanel();
        await loadTeamStats();
        await loadTeamPlayers(selectedTeam?.id || selectedTeam?.teamId);
    } catch (e) {
        console.error('❌ confirmTransfer:', e);
    }
}
```

**Kluczowe:** transfer to zawsze DWA osobne zapisy do `memberships/*` — dezaktywacja starego membershipu w `fromTeamId` + utworzenie nowego w `toTeamId`. To jest właśnie to, co sprawia że mechanizm znacznika (część B) łapie obie drużyny bez dodatkowego kodu.

---

## CZĘŚĆ B: Znacznik zmian dla delta-sync — pełny kod

Plik: `functions/index.js`, koniec pliku. Dwie nowe funkcje Cloud Functions v2 (region `europe-west1`, wdrożone jako osobne triggery, nie modyfikacja istniejących).

```js
/* ---------------------------------------------------------------
   2026-09-26 (uzgodnione z APP, sync #0038/#0039): Znacznik zmian dla delta-sync.
   Pola na teams/{teamId}:
     - membersChangedAt  — zmieniło się COKOLWIEK w składzie drużyny (dowolna rola)
     - playersChangedAt  — zmienił się skład ZAWODNIKÓW konkretnie
   To tylko znacznik "coś się zmieniło w tym zakresie" — przy niezgodności odśwież CAŁY zakres.
   Transfer zawodnika tworzy nowe membership w toTeamId i dezaktywuje stare w fromTeamId —
   oba zapisy przechodzą przez ten sam trigger, więc znacznik dostaje OBIE drużyny.
   --------------------------------------------------------------- */
async function _touchTeamMarkers(teamIds, fields) {
    const ids = [...new Set((teamIds || []).filter(Boolean))];
    if (!ids.length || !Object.keys(fields).length) return;
    await Promise.all(ids.map(id =>
        db.collection('teams').doc(id).set(fields, { merge: true }).catch(e =>
            console.error(`✗ _touchTeamMarkers teams/${id}:`, e))
    ));
}

// Pola membershipu istotne dla znacznika składu (usedSlot/cachedClubLicense/timestampy NIE liczą się).
const _MBR_MARKER_FIELDS = ['teamId', 'playerId', 'userId', 'role', 'status', 'displayName'];
function _mbrMarkerSnapshot(d) {
    if (!d) return null;
    const o = {};
    for (const k of _MBR_MARKER_FIELDS) o[k] = d[k] ?? null;
    return o;
}

exports.onMembershipWrittenTouchTeamMarker = onDocumentWritten('memberships/{membershipId}', async (event) => {
    const before = event.data?.before?.exists ? event.data.before.data() : null;
    const after  = event.data?.after?.exists ? event.data.after.data() : null;
    if (!before && !after) return;

    const beforeSnap = _mbrMarkerSnapshot(before);
    const afterSnap  = _mbrMarkerSnapshot(after);
    if (JSON.stringify(beforeSnap) === JSON.stringify(afterSnap)) return; // nic istotnego się nie zmieniło

    const ts = FieldValue.serverTimestamp();
    const beforeTeamId = before?.teamId || null;
    const afterTeamId  = after?.teamId || null;

    // membersChangedAt — dotyczy obu drużyn, jeśli membership przeniesiono (transfer)
    await _touchTeamMarkers([beforeTeamId, afterTeamId], { membersChangedAt: ts });

    // playersChangedAt — tylko gdy rola (przed LUB po) to ZAWODNIK
    const touchedForPlayers = new Set();
    if (before?.role === 'ZAWODNIK' && beforeTeamId) touchedForPlayers.add(beforeTeamId);
    if (after?.role === 'ZAWODNIK' && afterTeamId) touchedForPlayers.add(afterTeamId);
    if (touchedForPlayers.size) await _touchTeamMarkers([...touchedForPlayers], { playersChangedAt: ts });
});

// Pola playera istotne dla znacznika składu zawodników (edycja imienia/aktywności/przypisania do drużyn).
const _PLAYER_MARKER_FIELDS = ['name', 'firstName', 'lastName', 'isActive'];
function _playerMarkerSnapshot(d) {
    if (!d) return null;
    const o = {};
    for (const k of _PLAYER_MARKER_FIELDS) o[k] = d[k] ?? null;
    o.teams = (Array.isArray(d.teams) ? d.teams : []).map(t => `${t?.teamId}:${t?.status}`).sort();
    return o;
}

exports.onPlayerWrittenTouchTeamMarker = onDocumentWritten('players/{playerId}', async (event) => {
    const before = event.data?.before?.exists ? event.data.before.data() : null;
    const after  = event.data?.after?.exists ? event.data.after.data() : null;
    if (!before && !after) return;

    const beforeSnap = _playerMarkerSnapshot(before);
    const afterSnap  = _playerMarkerSnapshot(after);
    if (JSON.stringify(beforeSnap) === JSON.stringify(afterSnap)) return;

    const beforeTeamIds = (Array.isArray(before?.teams) ? before.teams : []).map(t => t?.teamId).filter(Boolean);
    const afterTeamIds  = (Array.isArray(after?.teams)  ? after.teams  : []).map(t => t?.teamId).filter(Boolean);
    const teamIds = [...new Set([...beforeTeamIds, ...afterTeamIds])];
    if (!teamIds.length) return;

    await _touchTeamMarkers(teamIds, { playersChangedAt: FieldValue.serverTimestamp() });
});
```

Odczyt po Waszej stronie: prosty listener na `teams/{teamId}.membersChangedAt` / `.playersChangedAt` (Firestore Timestamp). Jeśli macie lokalnie zapamiętany ostatni widziany timestamp i jest starszy niż ten na dokumencie — odśwież cały zakres tej drużyny. Dajcie znać jeśli wolicie inny kontrakt (np. webhook zamiast bezpośredniego listenera na `teams`).

---

## CZĘŚĆ C: Jak web liczy dostęp do licencji klubowej — pełny kod (dla Waszego bugu z ekranem blokady)

**Scenariusz który zgłosiłeś:** `club_orly_praga` miał `total=2=used`. Rafał ręcznie podbił `total` na 3 w konsoli Firestore. Cloud Function automatycznie przydzielił wolny slot `demo_rodzic_anna` (której własna subskrypcja indywidualna wygasała). Na web dostęp zadziałał poprawnie (ekran ustawień pokazuje 3/3, zalogowanie wpuszcza). Na appce ten sam user dostaje ekran blokady, mimo identycznych danych w Firestore (`memberships.usedSlot=1`, `clubs.license.used=3=total`).

### C.1 — Automatyczne przydzielenie slotu przy podniesieniu `total` (Cloud Function)

Plik: `functions/index.js`, `onClubLicenseUpdated` (trigger `onDocumentUpdated('clubs/{clubId}')`). To właśnie ta funkcja przydzieliła slot Annie automatycznie po zmianie `total`:

```js
exports.onClubLicenseUpdated = onDocumentUpdated('clubs/{clubId}', async (event) => {
    const before = event.data.before.data();
    const after  = event.data.after.data();
    if (!before || !after) return;

    const clubId   = event.params.clubId;
    const clubRef  = event.data.after.ref;
    const oldTotal = before.license?.total || 0;
    const newTotal = after.license?.total  || 0;
    // ... (scope change handling pominięte dla zwięzłości)

    // total wzrósł: przydziel wolne sloty kolejce kandydatów (trenerzy najpierw, potem rodzice,
    // sortowani po dacie wygaśnięcia trialu — najwcześniej uprawniony pierwszy)
    const used  = after.license?.used  || 0;
    const avail = newTotal - used;
    if (avail <= 0) return;

    // ... buduje listę kandydatów (memberships bez usedSlot, bez własnej subskrypcji,
    //     po 90-dniowym trialu, rola TRENER*/RODZIC, z uwzględnieniem scope/maxOneParentPerChild)

    let newUsed = used;
    const batch = db.batch();
    for (const c of sortedQueue) {
        if (newUsed - used >= avail) break;
        batch.update(c.m._ref, { usedSlot: 1, usedSlotUpdatedAt: FieldValue.serverTimestamp() });
        newUsed++;
    }
    if (newUsed > used) {
        batch.update(clubRef, { 'license.used': newUsed });
        await batch.commit();
    }

    // WAŻNE: po każdej zmianie total/scope, funkcja rozsyła "cachedClubLicense" na WSZYSTKIE
    // memberships tego klubu (nie tylko te ze zmienionym usedSlot!) — patrz C.2 niżej.
    await _writeCachedClubLicense(clubId, after.license);
});
```

### C.2 — `cachedClubLicense` — pole cache na KAŻDYM membershipie klubu

```js
const _buildClubLicenseCache = (licData) => {
    const raw = licData?.valid_until ?? licData?.expiresAt;
    return {
        validUntil: raw?.toDate ? raw.toDate() : (raw ? new Date(raw) : null),
        used:       licData?.used  ?? 0,
        total:      licData?.total ?? 0,
        updatedAt:  FieldValue.serverTimestamp(),
    };
};
const _writeCachedClubLicense = async (cid, licData) => {
    const snap = await db.collection('memberships').where('clubId', '==', cid).get();
    if (snap.empty) return;
    const cached = _buildClubLicenseCache(licData);
    // ... batched update: KAŻDY membership klubu dostaje TO SAMO pole `cachedClubLicense`
    //     (validUntil/used/total klubu — NIE per-osobowy usedSlot!)
    for (const chunk of chunks) {
        const b = db.batch();
        for (const d of chunk) b.update(d.ref, { cachedClubLicense: cached });
        await b.commit();
    }
};
```

**Uwaga:** `cachedClubLicense` to snapshot licencji KLUBU (agregat validUntil/used/total), zapisywany na wszystkich membershipach tego klubu — nie mówi nic o tym, czy TEN KONKRETNY membership ma przydzielony slot. To osobne pole od `usedSlot`. Jeśli appka czyta dostęp z `cachedClubLicense` zamiast (albo obok) `usedSlot`, może błędnie wnioskować "used=total, więc może nie ja mam slot" — **to jest nasza główna hipoteza waszego bugu, proszę sprawdźcie po swojej stronie skąd appka bierze decyzję o blokadzie ekranu.**

### C.3 — Jak web faktycznie decyduje o dostępie: `getAccessStatus()`

Plik: `coachay-core.js`. Pełna funkcja (skrócona o niezmienione sekcje P4/fallback dla zwięzłości, ale z KOMPLETNĄ sekcją P3 — kluczową dla tego przypadku):

```js
const GRACE_DAYS = 7;

async function getAccessStatus(uid, clubId, { claimSlot = false } = {}) {
    const now = new Date();
    // ...
    const clubDoc = await db.collection('clubs').doc(clubId).get();
    if (!clubDoc.exists) return _r('EXPIRED', 'no_club', null);
    const cd  = clubDoc.data();
    const lic = cd.license || null;   // ⚠️ CZYTANE BEZPOŚREDNIO z clubs/{clubId}.license, NIE z cachedClubLicense!

    const mDoc  = await _getMembershipForClub(uid, clubId);
    const mData = mDoc?.data() || {};
    const rawRole = mData.role || '';
    const role = rawRole === 'TRENER' ? (mData.trainerRole || 'TRENER_GLOWNY') : rawRole;

    if (role === 'ZAWODNIK') return _r('ACTIVE', 'player', null, 999);

    // P0.5: globalna subskrypcja indywidualna (RevenueCat, users.subscription) — sprawdzana PRZED
    // wszystkim innym. U Anny ta subskrypcja WYGASŁA, więc ten blok jej NIE przepuszcza (poprawnie).

    // P1: własna licencja (access_rights, źródło != 'individual') — Anna nie ma takiej, pomija.

    // P0: trial — Anna dawno po triale, pomija.

    // ── P3: Slot B2B klubowy (TO JEST TEN PRZYPADEK) ──────────────
    const TRENER_ROLES = ['TRENER_GLOWNY', 'TRENER_POMOCNICZY', 'TRENER', 'OWNER'];
    const canUseB2B = mDoc && _isRoleInB2BScope(role, lic) &&
                      (TRENER_ROLES.includes(role) || role === 'RODZIC');

    if (canUseB2B) {
        const licExpiry = lic?.valid_until?.toDate?.() ?? null;   // clubs.license.valid_until (14.10.2026 w tym przypadku)
        // Slot przydzielony przez CF (onClubLicenseUpdated) lub webhook
        if (mData.usedSlot === 1 && licExpiry) {
            if (licExpiry > now) return _r('ACTIVE', 'club_license', licExpiry);   // ← Anna trafia TUTAJ, dostaje ACTIVE
            return _r('EXPIRED', 'club_license_expired', null);
        }
        // Fallback: spróbuj pobrać slot przy logowaniu (gdyby CF nie zdążyła)
        if (claimSlot && licExpiry && licExpiry > now) {
            const claimed = await claimClubLicenseSlot(uid, clubId, mDoc);
            if (claimed.success) return _r('ACTIVE', claimed.source, claimed.expiryDate, claimed.daysLeft);
            return _r('EXPIRED', claimed.source, null);
        }
    }

    // P4: family (tylko KIBIC) — nie dotyczy roli RODZIC.
    // Fallback: clubs.licenseStatus (stare konta).

    // (jeśli nic z powyższego nie zwróciło ACTIVE, funkcja "spada" niżej i może zwrócić EXPIRED)
}

function _isRoleInB2BScope(role, lic) {
    if (!lic || !lic.valid_until) return false;
    const scope = lic.scope || 'all';
    if (scope === 'all') return true;
    if (scope === 'trainers_only') return ['TRENER_GLOWNY','TRENER_POMOCNICZY','TRENER','OWNER'].includes(role);
    if (scope === 'custom') return (lic.roles || []).includes(role);
    return false;
}
```

**Najważniejsza obserwacja dla Waszej strony:** decyzja P3 na webie zależy **wyłącznie** od dwóch pól:
1. `memberships/{id}.usedSlot === 1` (per-osobowy — czy TEN membership ma przydzielony slot),
2. `clubs/{clubId}.license.valid_until` (agregat klubu, czytany **bezpośrednio z `clubs`**, nie z jakiejkolwiek kopii/cache).

Pole `memberships/{id}.trialEndsAt` (u Anny: `31.12.2024`, przeterminowane — pozostałość po jej STAREJ licencji indywidualnej) **nigdy nie jest sprawdzane w ścieżce P3**. Jest używane tylko historycznie/gdzie indziej (np. przy przywracaniu trialu po transferze — patrz Część A, `oldTrialEndsAt`). Jeśli appka gdziekolwiek traktuje `trialEndsAt` jako ogólne "kiedy wygasa dostęp tego membershipu" — to jest podejrzany kandydat na przyczynę blokady, bo ta data jest przeterminowana, a mimo to nie powinna mieć znaczenia gdy `usedSlot===1`.

**Pytanie do Was:** czy możecie sprawdzić dokładnie, z którego pola/kolekcji appka bierze decyzję "zablokuj ekran" dla tego usera — `cachedClubLicense`, `trialEndsAt`, czy coś innego? Jeśli to `cachedClubLicense` — może potrzebować dodania per-membership `usedSlot` do tego obiektu, albo appka powinna zamiast tego czytać `usedSlot` osobno (tak jak web) i `clubs.license.valid_until` bezpośrednio.

---

## Numeracja

Następny wolny: **#0044**.
