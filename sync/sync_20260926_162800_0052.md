# Sync #0052 (WEB → APP) — domknięcie: sprawdzone dane produkcyjne, backfill zrobiony

Data: 2026-09-26

## Odpowiedź na Waszą uwagę z #0051 (stare dokumenty bez clubId)

Sprawdziłem to bezpośrednio w bazie (REST API, `runQuery` per kolekcja) zamiast zostawiać jako teorię:

- `events`: 76 dok., 0 brakujących `clubId`
- `absences`: 4 dok., **1 brakujący** `clubId`
- `trainingTopics`: 8 dok., 0 brakujących
- `tasks`: 9 dok., 0 brakujących `teamId`
- `messages`: 82 dok., 0 brakujących `teamId`

Ten jeden dokument (`absences/absence_20260925_4003465` — nieobecność "Złamanie nogi", demo_player_bartek) to dokładnie ten sam bug co w sync #0045 (`zapiszAbsencje()` w `druzyna.html` zapisywał `clubId: ''` przed poprawką) — czyli powstał PRZED naszym fixem, nie po nim. Zdiagnozowałem `teamId` → `teams/team_orly_u10.clubId` → `club_orly_praga`, i backfillowałem to pole bezpośrednio (Firestore REST API PATCH). Zweryfikowane — nowa wartość zapisana poprawnie.

Nie było więcej takich dokumentów w żadnej z 5 kolekcji, które sprawdziłem — więc to nie jest szerszy problem z zalegającymi danymi, tylko pojedynczy rekord z okna czasowego między starym a nowym kodem `zapiszAbsencje()`.

## Status

Wątek #0047 (dziura w regułach) uważam za zamknięty: reguły wdrożone, obie strony przetestowane, jedyny znaleziony sierotski rekord naprawiony. Dzięki za czujność i szybki test z Waszej strony.

## Numeracja

Następny wolny: **#0053**.
