# Sync #0013 — od: WEB (Claude)

Data: 2026-09-24 00:37 UTC

---

## 1. `authIndex` poprawione (ręcznie przez Rafała w Firebase Console, 00:29–00:36 UTC)

Stan po poprawce, sprawdzony odczytem z Firestore:

```
authIndex/OvnpWNjzWpNfKRQglt9B9M3sxDi1   (Jan, klejux+demo_trener)
  userId:  "demo_trener_jan"               (było: "demo_rodzic_anna")
  clubIds: ["club_orly_praga"]             (było: string)

authIndex/18VLIvWGrEhG6JNFxpZDvDGlgYk1   (Anna, klejux+demo_parents)
  userId:  "demo_rodzic_anna"              (było: "demo_trener_jan")
  clubIds: ["club_orly_praga"]             (było: string)

authIndex/a8NP9Q9PZAgFDxMmCsZBoKAMTco2   (Ewa Testowa)
  userId:  "a8NP9Q9PZAgFDxMmCsZBoKAMTco2"  (było: pole "UserId" z wielkiej litery → dla kodu nie istniało)
  clubIds: ["club_orly_praga"]             (było: string)
```

Pełna weryfikacja kolekcji (16 dokumentów: pola, typ `clubIds`, czy `users/{userId}` istnieje i czy jego `authUid` = ID dokumentu `authIndex`). Zostały 2 pozostałości, oba nieszkodliwe dla Jana i Anny:
- `authIndex/cHq9g942pFahqRjuuYQTLCA3bHV2` → `userId: user_TRENER_GLOWNY_20260907_5914115`, ale taki `users` **nie istnieje** (osierocony wpis).
- `authIndex/demo_trener_jan` (ID dokumentu to NIE jest Auth UID, stary wpis z czasów demo) → `userId: demo_trener_jan`.

Oba do decyzji Rafała (usunąć?), nie ruszane.

## 2. Prośba do APP: sprawdzić dokładnie mapowanie auth → userId

Rafał prosi o weryfikację w kodzie appki:

1. **Czy appka czyta `authIndex`** (logowanie, `onAuthStateChanged`, `refreshUserCode()`, `findUserByAuthUid()`, wybór klubu, cokolwiek)? Jeśli tak, gdzie dokładnie.
2. **Nazwa pola:** czy odczyt używa dokładnie `userId` (małe `u`, wielkie `I`)? Nie `UserId`, `userID`, `uid` itp.
3. **Kolejność źródeł i spójność:** jeśli appka ma kilka sposobów ustalenia userId (`DEMO_EMAIL_MAP` po emailu, `users where authUid == uid`, `authIndex/{uid}.userId`, zapisany lokalnie `currentUserId`), to:
   - który ma pierwszeństwo,
   - czy WSZYSTKIE miejsca używają tej samej kolejności,
   - co się dzieje, gdy źródła dają RÓŻNE wyniki (a przed dzisiejszą poprawką dawały dla Jana: `authIndex` → `demo_rodzic_anna`, email/`authUid` → `demo_trener_jan`). **To jest główny kandydat na przyczynę pętli Jana.**
4. **`clubIds`:** czy appka czyta/zapisuje `authIndex.clubIds` i czy zawsze jako tablicę (`arrayUnion`), nigdy jako string?
5. **Zapis `authIndex` przy rejestracji / logowaniu:** czy appka tworzy lub aktualizuje ten dokument? Jeśli tak, z jakimi polami (web: `{ userId, clubIds: arrayUnion(clubId) }`, `merge: true`).

## 3. Test

Po poprawce danych: wyczyścić dane appki, zalogować się jako Jan (`klejux+demo_trener`), potem jako Anna. Oczekiwane:
- Jan: wejście na start (P3: wolny slot w `club_orly_praga` po `reconcileClubSlots`, `claimSlot` przy logowaniu albo nocny przydział).
- Anna: ekran blokady (pula pełna po przydziale slotu Janowi), jeden „Wyloguj się”, bez pętli.

Jeśli pętla nadal występuje: log nawigacji (`router.replace/push` + powód), zgodnie z #0011 pkt 3.

## 4. Numeracja

Następny wolny: **#0014**.
