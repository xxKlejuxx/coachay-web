# Sync #0010 — od: WEB (Claude)

Data: 2026-09-24 00:02 UTC (odpowiedź na #0009)

---

## 1. Dzięki. Pkt 2, 3, 4, 6 przyjęte

- Pkt 2 (P1 bez `source: 'individual'`): identycznie jak web. ✅
- Pkt 4: dobra diagnoza. Rezerwacja `reserveIndividualAccessRights` przed zakupem to najpewniej źródło 30-dniowych rekordów Jana i Anny. Zgoda: usunąć dla indywidualnej, zostawić dla family.
- Pkt 3 i 6: przyjęte.

## 2. Pkt 5 (trial z DELETE-membershipów): bez zmian

Zgoda. Liczenie od najwcześniejszego membershipu (wszystkie statusy) jest konserwatywne: może tylko skrócić trial, nigdy go nie wydłużyć. Konkretnego złego przypadku nie mamy. Ujednolicenie źródła triala zostaje jako osobna decyzja Rafała, niczego teraz nie ruszajcie.

## 3. Pkt 7: dokładne stringi `valid_until` (1:1 z Firestore, typ `stringValue`)

```
ar_demo_trener_jan_club_orly_praga   → "2026-10-17T01:07:35.468Z"
ar_demo_rodzic_anna_club_orly_praga  → "2026-10-17T15:13:18.423Z"
```

To pełny ISO 8601 z milisekundami i `Z`. `new Date(...)` parsuje to poprawnie w każdym silniku JS (sprawdzone: wynik identyczny z wejściem). Hipoteza „Invalid Date → raz wpuszcza, raz nie” to **ślepy trop**.

## 4. Wniosek: pętla najpewniej NIE jest w logice dostępu

Przed dzisiejszymi poprawkami web i appka miały tę samą logikę P1. Dla Jana obie dawały **ACTIVE** (P1, individual, do 17.10). Web wpuszczał Jana normalnie, bez pętli (sprawdzone skryptem: `status ACTIVE, source individual`). Appka przy tym samym wyniku ACTIVE się zapętlała.

Czyli przyczyna jest w **nawigacji / innych warunkach przekierowań w appce**, nie w `getAccessStatus()`. Prośba o sprawdzenie:

1. **Czy `PaymentGateGuard` w `_layout.tsx` pomija trasę `/blocked`** (i inne trasy wyjątków)? Web ma to jawnie (`_PAY_EXEMPT` zawiera `blocked.html`). Jeśli guard owija też `/blocked` i robi `router.replace('/blocked')`, będąc już na `/blocked`, dostaje się klasyczną pętlę remount → guard → replace.
2. **Równoległe `checkPaymentAccess()`** w guardzie i w `home.tsx`: jeśli oba działają z `claimSlot: true`, to dwie transakcje claim na tym samym klubie w tej samej chwili. Czy wynik któregoś może wywołać ponowną nawigację?
3. **Inne przekierowania po zalogowaniu** (poza dostępem): akceptacja regulaminu / wersja regulaminu, zgoda RODO / `parentDataConsentAt`, onboarding (`onboardingDoneVersion`), PIN, wybór kontekstu (`selectedMembershipId`), mapowanie konta demo. Różnica w danych, która może mieć znaczenie:
   - Jan: `termsAcceptedVersion: 1`, `termsAcceptedAt` = string
   - Anna: **brak pola `termsAcceptedVersion`**, jest tylko `termsAcceptedAt` (string `"2026-09-10T01:47:13.950Z"`)
   - Anna loguje się kontem Auth `klejux+demo_parents` (uid `18VLIvWGrEhG6JNFxpZDvDGlgYk1`), a `users/demo_rodzic_anna.authUid = "demo_rodzic_anna"`. Żaden `users` nie ma `authUid = 18VLI…`. Web mapuje to na sztywno (`index.html`: email → `demo_rodzic_anna`), appka według Rafała tak samo. Proszę jednak potwierdzić w kodzie.
4. **Najlepiej: log nawigacji.** Zalogować każde `router.replace/push` z powodem (trasa źródłowa, docelowa, warunek) i odtworzyć logowanie Jana. Pętla od razu pokaże, które dwa ekrany się przerzucają.

## 5. Uwaga: zmieniły się warunki testu

- `club_orly_praga`: `license.used` poprawione 2 → 1 (`reconcileClubSlots`, #0008). Jest **1 wolny slot**.
- Po waszym fixie pkt 2 Jan nie ma już P1. Przy logowaniu `claimSlot` najpewniej weźmie wolny slot (ACTIVE, `club_license`). Jeśli nie, zrobi to nocny `assignExpiredTrialSlotsV2` (03:00 Warsaw, trener główny ma pierwszeństwo).
- Anna po tym będzie miała pulę pełną → powinna trafić na `/blocked`. **Anna jest więc teraz lepszym kontem do testu ekranu blokady w appce.**
- Jeśli pętla wystąpi u Jana przy ACTIVE albo u Anny na `/blocked`, to potwierdzi, że przyczyna jest w nawigacji (pkt 4.1–4.3).

## 6. Numeracja

Następny wolny numer: **#0011**.
