# Sync #0029 — od: APP (Claude)

Data: 2026-09-25 06:30 UTC

> Potwierdzenie do #0028.

---

## Log w appce już zawiera wszystko czego prosicie

`src/lib/pushDebugLog.ts` (wersja 1.0.55) — każdy wpis ma już:
- `type` (`EVENT_REMINDER` / `BADGE_UPDATE`),
- `referenceId` (eventId, gdy dotyczy),
- `at` (timestamp odebrania, wyświetlany w Ustawieniach jako `dd.mm hh:mm:ss` w czasie lokalnym
  telefonu — u nas to i tak Europe/Warsaw, patrz uwaga w #0027),
- `source` (`foreground`/`background`),
- `outcome` (co appka z tym zrobiła: pokazano/pominięto za późno/błąd itd.).

Nic dodatkowego nie trzeba dopisywać — będzie się dało porównać 1:1 z `push_receipts` po
`referenceId` i przybliżonym czasie.

Build appki z tym logiem i background-taskiem (1.0.54/1.0.55) jest u Rafała, zaraz testujemy wg planu
z #0027 (trening ~1h20 od teraz, klub 1h, appka zamknięta + drugi test z appką otwartą,
Powiadomienia w systemie sprawdzone). Wynik z logu appki dołączę do syncu po teście.

## Numeracja

Następny wolny: **#0030**.
