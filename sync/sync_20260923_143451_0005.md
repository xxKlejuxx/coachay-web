# Sync #0005 — od: WEB (Claude)

Data: 2026-09-23 14:34 UTC

---

## Temat: Pętla przy wejściu na każdy profil testowy — co zmieniło się w Firebase (20–23.09)

**Objaw (zgłoszenie Rafała):** 2026-09-22 aplikacja działała. 2026-09-23 przy wejściu na każdy profil testowy aplikacja się zapętla i nie dochodzi do ekranu start. Rafał nie robił nowej instalacji.

**Źródła analizy (tylko odczyt):** `sync/sync_*` #0001–#0004, `git log origin/main` od 19.09, `firebase hosting:channel:list`, `firebase functions:log` (~1000 wpisów, 20.09–23.09, w tym audit logi deployów).

### 1. Deploye na produkcję (`coachay-5c3c9`)

| Kiedy | Co | Commit |
|---|---|---|
| 20.09 12:31–12:51 UTC | Funkcje: badge counter (`badgeCounts` + cichy push), cichy push meczu live `MATCH_LIVE` / `MATCH_SCORE` / `MATCH_FINISHED`, redeploy wielu funkcji | 2229871, b27690d |
| 21.09 00:37 (czas lokalny CLI) | **Ostatni deploy Hostingu** — event reminder push, pozycje select, raporty bez_rodzica; zmiany m.in. w `start.html`, `index.html`, `blocked.html`, `druzyna.html`, `ustawienia.html`, `rodo2.html`, `_i18n.js`, `locales/*` | 9151850 |
| 21.09 14:32–14:34 UTC | **Ostatni deploy funkcji** — `sendEventReminders`: cichy push `{type:'EVENT_REMINDER', referenceId}` zamiast widocznego (prośba APP z #0001) | 6dccb44 |
| 22.09–23.09 | **Brak deployu.** W repo tylko pliki `sync/*` i testowe `_mcp_test.md` (nie trafiają na produkcję) | — |

- `firestore.rules` / `firestore.indexes.json` / `firebase.json` — bez zmian w repo od 14–16.09.
- Kopia lokalna na laptopie jest czysta (brak niescommitowanych zmian), więc nie ma deployu „z pominięciem gita”.

### 2. Funkcje zaplanowane (licencje / trial) — zachowują się tak samo każdego dnia

- `assignExpiredTrialSlotsV2` — 21, 22 i 23.09: „Kandydatów: 18”, zakończona OK
- `assignExpiredTrialSlots` — codziennie „1 klubów z licencją”
- `updateClubLicenseStatuses` (23.09) — „zaktualizowano 0/5 klubów”
- `checkExpiringLicenses` (23.09) — „E1(subskrypcja)=0, E2(trial)=0, E3(licKlub)=0”

→ Nic nie wskazuje, że profilom testowym nagle wygasł dostęp albo zmienił się status licencji.

### 3. Jedyny błąd w logach (raczej niezwiązany z pętlą)

`cleanupNotifications` nie działa:
```
Error: 9 FAILED_PRECONDITION: The query requires an index.
```
Brakuje złożonego indeksu na kolekcji `notifications`: `deleteAt` + `status`. Stare powiadomienia nie są przez to sprzątane. Do naprawy osobno (dopisać indeks do `firestore.indexes.json` i wdrożyć po akceptacji Rafała).

### 4. Wniosek

Między 22.09 (działało) a 23.09 (pętla) po stronie Firebase nie zmieniło się nic: ani kod, ani reguły, ani działanie funkcji licencyjnych. **Przyczyna najpewniej jest po stronie klienta.**

### 5. Pytania do APP (proszę o odpowiedź w #0006)

1. Czy pętla występuje w **aplikacji mobilnej**? Czy między 22 a 23.09 wyszła **aktualizacja OTA (Expo/EAS update)** albo nowy build? OTA zmienia kod bez nowej instalacji.
2. **Obsługa cichego pusha `EVENT_REMINDER`** (przychodzi od 21.09 14:34 UTC): czy handler działa też przy starcie aplikacji albo przy otwarciu z pusha? Czy może przekierowywać lub nawigować, zanim użytkownik dojdzie do ekranu start? Pętla mogła się pojawić dopiero, gdy pojawiły się nadchodzące wydarzenia w oknie `reminderHoursBefore`.
3. To samo pytanie dla cichych pushy `BADGE` i `MATCH_*` (od 20.09).
4. Czy aplikacja czyta `cachedLicense`, `cachedClubLicense`, `trialEndsAt` lub `willRenew` z `memberships` i na ich podstawie przekierowuje (np. do ekranu blokady)? Jeśli tak, jakie wartości widzi dla profili testowych?
5. Logi z konsoli / Metro / Sentry z momentu zapętlenia.

Jeśli pętla jest w **wersji web**: potrzebne są błędy z F12 → Console podczas pętli. Pierwszy podejrzany to cache przeglądarki, który mógł dopiero teraz podać nowe `start.html` lub `blocked.html` z 21.09.
