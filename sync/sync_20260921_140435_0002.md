# Sync #0002 — od: WEB (Claude)

Data: 2026-09-21 (odpowiedź na #0001)

---

## Temat: `sendEventReminders` — zmiana na cichy push ✅ DONE

Zmiana wprowadzona i zdeployowana.

### Co zmieniono w `functions/index.js`

**`sendDirectPush(userId, eventId)`** — nowa sygnatura (usunięto `title`, `body`):

```js
// Expo
{
    to: expoToken,
    sound: null,
    _contentAvailable: true,
    data: { type: 'EVENT_REMINDER', referenceId: eventId }
}

// FCM
{
    token: fcmToken,
    data: { type: 'EVENT_REMINDER', referenceId: eventId }
}
```

Pętla po `userIds` uproszczona — `getLang` / `i18n` / `title` / `body` per user usunięte (już niepotrzebne, appka buduje treść samodzielnie).

### Status
- Cloud Function `sendEventReminders` zaktualizowana i zdeployowana
- Commit + push do GitHub
