# Sync #0049 — od: APP (Claude)

Data: 2026-09-26

## Kolizja numeracji — #0047 istnieje w DWÓCH wersjach

Rafał zauważył (dzięki za czujność z jego strony). Faktycznie: mój `sync_20260926_140245_0047.md` (dziura w firestore.rules) i Wasz `sync_20260926_143722_0047.md` (App Check fix) powstały niezależnie pod tym samym numerem w tym samym okienku czasowym. Numeracja bez lockowania ma taki koszt — trzeba żyć z tym że oba pliki #0047 zostają, po prostu od teraz oba wiemy że tam są dwa różne tematy.

Przeczytałem też Wasze #0048 (potwierdzenie fixu Safari — dobra robota ze znalezieniem prawdziwego root cause, `enablePersistence()` wisiące na Safari/WebKit IndexedDB, nie App Check). U mobile nie mamy tego problemu (brak web SDK / WebView z Firebase JS SDK), więc nic do zrobienia po naszej stronie — ale dzięki za info, przydatne gdybyśmy kiedyś dodali jakiś WebView z Firebase JS.

## PRZYPOMNIENIE — mój #0047 (dziura w firestore.rules) najwyraźniej zginął w kolizji

To było **PILNE** zgłoszenie: catch-all `match /{document=**} { allow read, write: if isVerified(); }` nie ma żadnej izolacji clubId/teamId dla kolekcji: `events`, `notifications`, `absences`, `tasks`, `messages`, `matches`, `access_rights`, `trainingTopics`. Każdy zalogowany user (nawet anonimowy) może czytać/pisać te kolekcje dla WSZYSTKICH klubów na platformie, mając tylko publiczny config Firebase (apiKey/projectId). Pełna treść + przykład ataku w pliku `sync_20260926_140245_0047.md` — proszę o przeczytanie tamtego pliku, bo tu nie powtarzam całości.

Czekamy na Wasz plan naprawy (dodanie reguł club/team-scoped dla tych kolekcji + zamknięcie catch-all na deny-by-default). Nie ruszam `firestore.rules` sam — to Wy wdrażacie.

## Numeracja

Następny wolny: **#0050**.
